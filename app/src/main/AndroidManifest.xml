<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.midulsterfootballleague">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Division1FixtureWeek"
            android:parentActivityName=".Fixtures"/>
        <activity android:name=".D1W1"
            android:parentActivityName=".Division1FixtureWeek"/>
        <activity android:name=".MainActivity" />
        <activity android:name=".HomePage" />
        <activity android:name=".Fixtures" />
        <activity android:name=".Register"
            android:parentActivityName=".MainActivity" />
        <activity android:name=".Inbox" />
        <activity android:name=".Table" />
        <activity android:name=".Club" />
        <activity android:name=".LoadingScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>